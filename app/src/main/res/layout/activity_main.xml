<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/turnon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Turn on Blutooth"/>
    <Button
        android:id="@+id/paireddevices"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/turnon"
        android:text="Get paried devices details"/>
    <Button
        android:id="@+id/ScanBluetoothDevices"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/paireddevices"
        android:text="Scan nearby Bluetooth Devices"
        />
    <Button
        android:id="@+id/MakeDeviceDiscoverable"
        android:layout_below="@id/ScanBluetoothDevices"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Make device visible to others"
        />
    <ListView
        android:id="@+id/ListviewForScannedDevices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/MakeDeviceDiscoverable"
        />
</RelativeLayout>